<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="rendes.list">
      <field name="name">rendes list</field>
      <field name="model">rendes.rendes</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="rendes.action_window">
      <field name="name">rendes window</field>
      <field name="res_model">rendes.rendes</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="rendes.action_server">
      <field name="name">rendes server</field>
      <field name="model_id" ref="model_rendes_rendes"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="rendes" id="rendes.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="rendes.menu_1" parent="rendes.menu_root"/>
    <menuitem name="Menu 2" id="rendes.menu_2" parent="rendes.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="rendes.menu_1_list" parent="rendes.menu_1"
              action="rendes.action_window"/>
    <menuitem name="Server to list" id="rendes" parent="rendes.menu_2"
              action="rendes.action_server"/>
-->
  </data>
</odoo>